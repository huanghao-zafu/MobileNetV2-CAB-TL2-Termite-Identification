# Reference environment — matches manuscript Table 2 exactly.
# Python 3.10.19 (conda env "test") · CUDA 11.8 · NVIDIA GeForce RTX 3050 Laptop (4 GB)
#
# NOTE: verify these pins on the machine that produced the reported results with
#   pip freeze > environment-freeze.txt
# and commit that file alongside this one before tagging the release.
#
# torch 2.0.1 pairs with torchvision 0.15.2 and requires numpy < 2 (ABI).

torch==2.0.1
torchvision==0.15.2
numpy==1.26.4
pandas>=2.0
scipy>=1.10
scikit-learn>=1.2
opencv-python>=4.7
pillow>=9.5
imageio>=2.31
matplotlib>=3.7
seaborn>=0.12
tqdm
openpyxl>=3.1
PyYAML>=6.0
timm>=0.9
